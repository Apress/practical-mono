using System;
public class PrimaryOperators
{

    public static void()
    {

        Person p = new Person();
        Bool result = ( p is Person );
        Bool result typeof
        int size_of_age = sizeof(p.age);               // Returns the size of the ‘age’ member variable
        short a = 32767;                                       // a cannot hold more than 32767 as a value
        short b = checked( a + 1 );                       // checked and so overflow will raise an exception
        short c = unchecked( a + 1 );                   // unchecked and no exception is raised
        Console.Out.WriteLine(“{0}”, typeof(a));  // Should return ‘System.Short’

    }

}
